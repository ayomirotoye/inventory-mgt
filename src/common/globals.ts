export const nairaFormatter = new Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'NGN',
});

export const alertTimeoutInMs = 5000;
export const userInfoKey = "userDetails"
export const PageSize = 10;